<%= simple_form_for(@excavation) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :dai_gazetter_id %>
    <%= f.input :shortcut %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
